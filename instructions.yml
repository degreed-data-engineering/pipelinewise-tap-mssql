I'm using this Meltano extractor in one of my ETL pipelines.
I need to extract a new table called dbo-schedules but I'm facing some issues.

(1) during the first extraction, the issue is on the loader side (that is, target-snowflake) and it raises this error:
```DML operation to table GBORTOLUSSI.DBO_SCHEDULE failed on column EXECUTIONTIME with error: Can't parse '01:00:00' as timestamp with format 'AUTO'```.
The table that is created on Snowflake has the column EXECUTIONTIME as type TIMESTAMP_NTZ.

(2) if I try to extract the table again, the issue is on the tap side and it raises this error:
```TypeError: datetime.time(9, 0) is not JSON serializable``` and it is still related to the ExecutionTime column.

If I run a discovery with tap-mssql, the column ExecutionTime presents these results:
```
          "ExecutionTime": {
            "inclusion": "available",
            "format": "date-time",
            "type": [
              "null",
              "string"
            ]
          },
```